# My Game Engine

## How to build

Dependencies:

* SDL3
* ImGui (optional)

Instructions:

```bash
git clone https://github.com/uosq/nav-engine.git
cd nav-engine
./compile.sh
```

> [!NOTE]
> compile.sh has a optional parameter: imgui

```bash
./compile.sh imgui # compiles with ImGui enabled
```